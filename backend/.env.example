PORT=3000

# Used by `npm run sync` (scripts/syncDatabase.ts). The service-role key
# bypasses Row Level Security, so keep .env local — never ship it to the
# browser or commit it.
SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
